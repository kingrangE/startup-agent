# SePark 기여 가이드

SePark에 기여해 주셔서 감사합니다. 이 저장소는 아직 개발 기준선 단계이므로, 구현된 동작과 계획 중인 기능을 구분하고 재현 가능한 작은 변경을 우선합니다.

## 시작하기 전에

- 큰 기능, 공용 API 변경, 새 의존성은 구현 전에 이슈에서 범위와 소유 파일을 합의합니다.
- 기존 `LeanCanvas`, `CanvasEvaluation`, `LLMClient`의 공개 시그니처를 호환성 검토 없이 바꾸지 않습니다.
- 실제 OpenAI 평가 실행은 비용과 외부 데이터 전송을 수반합니다. 명시적인 승인과 예산 없이 실행하지 않습니다.
- API 키, `.env`, 캐시, 생성 원문, 개인정보를 커밋하지 않습니다.
- 아직 오픈소스 라이선스가 정해지지 않았으므로 외부의 큰 기여는 먼저 관리자와 라이선스 조건을 확인합니다.

## 개발 환경

Python 3.10 이상을 사용합니다.

```bash
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install --upgrade pip
python3 -m pip install -r requirements.txt
python3 -m pip install "PyYAML>=6.0" "pytest>=8.0"
```

현재 기준선의 `requirements.txt`에는 PyYAML과 pytest가 아직 포함되지 않아 별도 설치가 필요합니다.

## 브랜치와 변경 범위

하나의 브랜치는 하나의 목적만 다룹니다. 다음 접두사를 권장합니다.

- `feat/`: 사용자 기능
- `fix/`: 버그 수정
- `test/`: 테스트 보강
- `docs/`: 문서 변경
- `chore/`: 패키징, CI, 저장소 관리

동시에 여러 작업이 진행될 때는 독립 worktree를 사용하고, 다른 작업자가 소유한 파일은 직접 수정하지 않습니다. 공용 계약이나 의존성 변경이 필요하면 해당 소유자에게 변경 이유와 필요한 인터페이스를 전달합니다.

## 구현 원칙

- 외부 서비스는 `LLMClient` 같은 경계 뒤에 두고 테스트에서는 fake를 주입합니다.
- LLM 응답은 성공을 가정하지 말고 스키마와 오류 경로를 검증합니다.
- 입력과 출력에 개인정보나 검증되지 않은 사실이 포함될 수 있음을 고려합니다.
- 사용자에게 보이는 문서에서는 현재 기능과 예정 기능을 명확히 나눕니다.
- 수치, 품질, 비용 또는 성능을 주장할 때는 재현 가능한 결과 파일과 실행 조건을 함께 제시합니다.

## 검증

코드 변경은 최소한 다음 검증을 통과해야 합니다.

```bash
python3 -m pytest -q
git diff --check
```

전체 테스트가 judge factory import 오류로 수집되지 않는다면 Foundation의 factory 복구가 빠졌는지 먼저 확인합니다. 관련 복구가 병합되기 전에는 이 실패를 PR에 명시하고, 변경과 직접 관련된 테스트를 별도로 실행합니다. 실패를 숨기기 위해 테스트를 건너뛰거나 삭제하지 않습니다.

문서 변경은 다음도 확인합니다.

- 링크 대상 파일이 실제로 존재하는가?
- 예시 명령의 옵션이 현재 CLI `--help`와 일치하는가?
- 공개되지 않은 데모 URL이나 생성되지 않은 결과를 제시하지 않았는가?
- 유료 API 명령과 API 없는 단위 테스트가 구분되어 있는가?

## 평가 실험

평가 방법과 메트릭은 [docs/evaluation.md](docs/evaluation.md)를 먼저 읽어 주세요.

유료 평가를 승인받아 실행할 때는 작은 표본으로 시작합니다.

```bash
python3 -m evals --help
python3 -m evals run-verdict-accuracy --limit 1
```

PR에는 키를 제외하고 다음 정보를 남깁니다.

- 실행한 커밋 SHA
- 생성 모델과 judge 모델
- 데이터셋 및 표본 수
- 캐시 사용 여부
- 실행 명령
- 통과와 실패를 모두 포함한 결과
- 알려진 오류나 재시도

실제 결과가 없으면 예상값을 결과처럼 작성하지 않습니다.

## 커밋

리뷰 가능한 기능 단위로 커밋하고, 명령형의 짧은 제목을 사용합니다.

```text
docs(README): clarify current product scope
test(evaluation): cover verdict threshold boundary
fix(factory): wire pairwise judge
```

포맷 변경과 동작 변경, 생성 파일과 원본 변경을 한 커밋에 섞지 않습니다.

## Pull Request 체크리스트

- [ ] 변경 이유와 사용자 영향을 설명했다.
- [ ] 변경 범위 밖의 파일을 수정하지 않았다.
- [ ] 관련 테스트를 추가하거나 문서 전용 변경임을 설명했다.
- [ ] 실행한 검증 명령과 결과를 적었다.
- [ ] 외부 API 호출 여부와 비용 발생 여부를 적었다.
- [ ] 새 환경변수, 의존성 또는 마이그레이션을 문서화했다.
- [ ] 비밀값, 캐시, 생성 원문과 개인정보가 포함되지 않았다.
- [ ] 현재 한계와 남은 위험을 숨기지 않았다.

PR은 가능한 한 작게 유지합니다. 공용 계약 변경이나 후속 작업이 필요하면 현재 PR에 몰아넣지 말고 별도 이슈로 연결합니다.
